<ul class="list-unstyled">
  <% tasks.each do |task| %>
    <li class="media mb-3">
      <div class="media-body">
        <div>
          <%= link_to task.user.name, task %> <span class="text-muted">posted at <%= task.created_at %></span>
        </div>
        <div>
          <p>タスクステータス：<%= task.status %></p>
          <p>タスク内容：<%= task.content %></p>
        </div>
      </div>
    </li>
  <% end %>
  <%= paginate tasks %>
</ul>